# ST-Manager-serverplugin

SillyTavern 资源管理与自动化工具 - 完整集成插件

## ✨ 功能

### 📊 资源管理
- **角色卡管理** - 浏览、搜索、编辑角色卡
- **世界书管理** - 查看和管理世界书条目
- **预设管理** - 管理预设及其绑定正则
- **正则脚本管理** - 全局和预设绑定正则管理

### 💾 备份与恢复
- **一键备份** - 支持全量和增量备份
- **定时备份** - 每日/每周自动备份
- **备份恢复** - 从历史备份恢复数据
- **过期清理** - 自动清理过期备份

### ⚙️ 自动化规则
- **条件匹配** - 多条件组合匹配角色卡
- **批量操作** - 批量添加标签、移动文件夹
- **规则预览** - 执行前预览匹配结果
- **规则集管理** - 保存和复用规则集

## 📦 安装

### 方式 A：作为 SillyTavern 插件（推荐）

#### 1. 启用服务端插件

编辑 SillyTavern 的 `config.yaml`：

```yaml
enableServerPlugins: true
```

#### 2. 克隆插件仓库

```bash
cd SillyTavern/plugins
git clone https://github.com/Youzini-afk/ST-Manager-serverplugin.git
```

#### 3. 安装依赖

**Windows (PowerShell):**
```powershell
cd ST-Manager-serverplugin
.\install.ps1
```

**Linux/Mac:**
```bash
cd ST-Manager-serverplugin
chmod +x install.sh
./install.sh
```

或手动安装：
```bash
cd ST-Manager-serverplugin
npm install
```

#### 4. 重启 SillyTavern

重启后：
- ✅ 服务端插件自动加载
- ✅ 前端扩展自动安装到 `public/scripts/extensions/ST-Manager/`
- ✅ 前端文件已预构建，无需手动 npm build

控制台会显示：
```
═══════════════════════════════════════════════
  🌐 ST Manager Web UI 已启动
═══════════════════════════════════════════════
  ✅ Web UI: http://localhost:5000
  📡 SillyTavern API: http://localhost:8000/api/plugins/st-manager
═══════════════════════════════════════════════
```

#### 5. 访问 Web UI

- **完整 Web UI**: http://localhost:5000 （功能完整的资源管理界面）
- **酒馆扩展面板**: 在酒馆 Extensions 标签页中启用 ST Manager（精简版）

---

### 🔄 更新插件

```bash
cd SillyTavern/plugins/ST-Manager-serverplugin
git pull
npm install  # 更新依赖
# 然后重启 SillyTavern
```

---

### 🔧 开发者：重新构建前端

仅在修改前端代码时需要：
```bash
cd ST-Manager-serverplugin/client
npm install
npx vite build
```

## 📁 目录结构

```
ST-Manager-serverplugin/
├── package.json           # 插件元信息
├── README.md              # 本文档
├── server/                # 服务端插件
│   ├── index.js           # 入口文件
│   ├── modules/           # 功能模块
│   │   ├── backup.js      # 备份模块
│   │   ├── cards.js       # 角色卡管理
│   │   ├── presets.js     # 预设管理
│   │   ├── regex.js       # 正则脚本管理
│   │   ├── extensions.js  # 扩展管理
│   │   ├── worldInfo.js   # 世界书管理
│   │   ├── resources.js   # 资源聚合
│   │   ├── automation.js  # 自动化规则
│   │   └── config.js      # 配置管理
│   └── webui/             # 完整 Web UI（一比一复刻 Python 版）
│       ├── api.js         # API 兼容层
│       ├── static/        # 静态资源（CSS, JS, 图片）
│       └── templates/     # Nunjucks 模板（Jinja2 兼容）
└── client/                # 酒馆扩展面板（精简版）
    ├── src/               # 源码
    ├── dist/              # 构建产物
    └── package.json       # 前端依赖
```

## 🔌 API 端点

所有端点前缀：`/api/plugins/st-manager`

### 基础

| 端点 | 方法 | 描述 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/stats` | GET | 资源统计 |
| `/config` | GET/POST | 获取/更新配置 |

### 资源

| 端点 | 方法 | 描述 |
|------|------|------|
| `/cards/list` | GET | 角色卡列表 |
| `/worldbooks/list` | GET | 世界书列表 |
| `/presets/list` | GET | 预设列表 |
| `/presets/regex/:presetId` | GET | 预设绑定的正则 |

### 正则脚本（全局与预设绑定）

| 端点 | 方法 | 描述 |
|------|------|------|
| `/regex/global` | GET | 获取全局正则（从 settings.json） |
| `/regex/scripts` | GET | 列出正则脚本文件 |
| `/regex/aggregate` | GET | 汇总全局 + 预设绑定正则 |
| `/regex/extract-from-preset` | POST | 从预设数据提取正则 |
| `/regex/list` | GET | 正则脚本列表（兼容路由） |

### 备份

| 端点 | 方法 | 描述 |
|------|------|------|
| `/backup/trigger` | POST | 触发备份 |
| `/backup/list` | GET | 备份列表 |
| `/backup/restore` | POST | 恢复备份 |
| `/backup/delete` | DELETE | 删除备份 |
| `/backup/schedule` | GET/POST | 备份计划 |

### 自动化

| 端点 | 方法 | 描述 |
|------|------|------|
| `/automation/rulesets` | GET | 规则集列表 |
| `/automation/ruleset/:id` | GET | 获取规则集 |
| `/automation/ruleset` | POST | 保存规则集 |
| `/automation/ruleset/:id` | DELETE | 删除规则集 |
| `/automation/preview` | POST | 预览规则 |
| `/automation/execute` | POST | 执行规则 |

## 🛠️ 开发

### 模块说明

**regex.js** - 正则脚本管理核心模块

完整复刻 Python 后端的正则处理逻辑，支持：
- 全局正则：从 `settings.json` 中提取各种格式的全局正则规则
- 预设绑定正则：从预设文件中提取 `extensions.regex_scripts` 等字段
- 正则标准化：支持多种格式的正则条目自动标准化
- 正则去重：基于 pattern/flags/replace 的智能去重
- 正则聚合：汇总全局 + 预设绑定的所有正则规则

### 构建前端

```bash
cd client
npm install
npm run build
```

### 调试

服务端日志会输出到 SillyTavern 控制台，前缀为 `[ST Manager]`。

## 📄 License

MIT

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

- GitHub: https://github.com/Youzini-afk/ST-Manager-serverplugin
